[tool.poetry]
name = "execubot"
version = "1.1.0"
description = "bot for run command on host"
authors = ["kolo <sabatt148@gmail.com>"]
license = "MIT"
readme = "README.md"

[tool.poetry.dependencies]
python = "^3.12"
aiogram = "^3.15.0"
Telethon = "^1.38.1"

[tool.poetry.group.dev.dependencies]
ruff = "^0.8.1"

[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"
